import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Weather to Stay",
  description: "Discover properties with perfect weather conditions for your stay",
  keywords: ["weather", "properties", "accommodation", "travel", "forecast"],
  authors: [{ name: "Weather to Stay" }],
  manifest: '/manifest.json',
  icons: {
    icon: '/weather-icon.svg',
    shortcut: '/weather-icon.svg',
    apple: '/weather-icon.svg',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
